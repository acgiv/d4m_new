import 'package:flutter/material.dart';

class DocumentoScreen extends StatelessWidget {
  const DocumentoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("documenti"));
  }
}
